# backend will send calls to database

import os
import requests
import json

class _pool_database:
	
	# initialize databse
	def __init__(self):
		# TODO: initialize database
		x = 1 # placeholder

	# set a new pool
	def set_pool(self, pool_id, data):
		# TODO: add pool_id with data to database
		x = 1 # placeholder

	# get a specific pool by id
	def get_pool(self, pool_id):
		# TODO: get pool by id
		x = 1 # placeholder

	# remove pool from database
	def delete_pool(self, pool_id):
		# TODO: delete pool according to id
		x = 1 # placeholder
